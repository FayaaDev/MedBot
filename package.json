{
  "name": "medbot",
  "private": true,
  "type": "module",
  "scripts": {
    "clear:kv": "node scripts/clear-app-kv.mjs",
    "check": "node --check src/index.js",
    "dev": "npx wrangler dev --test-scheduled",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
